---
paths:
  - "**/.claude/**/*.md"
---

# Bungafy après modification

Après avoir modifié un fichier Markdown (`.md`) dans le répertoire `.claude` (règles, skills, agents, CLAUDE.md), invoquer immédiatement le skill `bungafy` sur ce fichier pour réduire le nombre de tokens.
